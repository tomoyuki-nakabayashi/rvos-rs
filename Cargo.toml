[package]
name = "rvos-rs"
version = "0.1.0"
authors = ["tomoyuki-nakabayashi <tomo.wait.for.it.yuki@gmail.com>"]

[lib]
crate-type = ["staticlib"]

[dependencies]
rlibc = "1.0"
bare-metal = "0.2.4"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"